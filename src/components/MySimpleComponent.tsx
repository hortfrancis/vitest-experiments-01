// Example React component to be tested
// It just renders some text inside a <div> element. 
const MySimpleComponent = ({ text }: { text: string }) => {
  return <div>{text}</div>;
};

export default MySimpleComponent;
